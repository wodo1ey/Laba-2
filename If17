#include "iostream"
using namespace std;

void main() {
	float a, b, c;
	cin >> a >> b >> c;
	if ((a < b && b < c) || (a > b && b > c)) cout << 2 * a << endl << 2 * b << endl << 2 * c;
	else cout << (-1) * a << endl <<  (-1) * b << endl << (-1) * c;
}
